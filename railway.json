{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python reset_admin_production.py && gunicorn Sat_Shine.wsgi:application --bind 0.0.0.0:$PORT"
  }
}